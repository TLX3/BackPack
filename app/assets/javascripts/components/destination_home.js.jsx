(function(root) {
  'use strict';
  root.DestinationHome = React.createClass({
    render: function () {
      return (
        <RB.Grid>
          <h2>Groups</h2>
        </RB.Grid>
      );
    }
  });
}(this));
